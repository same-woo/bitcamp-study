헬스장은 기본 헬스장 이용 손님이 있다.
헬스장 인포 시스템은 손님 정보를 나타내거나 새로운 손님을 추가할 수 있다.

기본정보는 성명,나이,성별,헬스 잔여일수, 락카이용 여부 등이 있다.
기본 10명이 등록되어 있으며 추가를 통해서 데이터를 증가시킬 수 있다.

!데이터 조회(Y) or 추가(n) ? (Y/n)

성명,나이,성별,헬스 잔여일수, 락카 이용 여부 등록


실행방법:
root@bitcamp-web:~/bitcamp-study/bitcamp-study/java-lang/app (경로)
;  javac -cp bin/main -d bin/main src/main/java/bitcamp/project/app/DefaultMembers.java
; javac -cp bin/main -d bin/main src/main/java/bitcamp/project/app/MemberManager.java
;  javac -cp bin/main -d bin/main src/main/java/bitcamp/project/app/MainClass.java
; java -cp bin/main bitcamp.myapp.MainClass

